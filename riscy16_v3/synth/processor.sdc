# Add target-specific clock, IO, and timing constraints here.
#
# The DE1-SoC FPGA wrapper is maintained under ../../hardware/fpga and uses the
# board-level Quartus project files in that directory. This file is intentionally
# tool-neutral until a processor-only synthesis target is selected.
